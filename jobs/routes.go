package jobs

// import (
// 	"github.com/pagodabox/nanobox-boxfile"
// 	"github.com/pagodabox/nanobox-router"
// )

// func configureRoutes(box boxfile.Boxfile) {
// 	webs := box.Nodes("web")
// 	for _, web := range webs {
// 		b := box.Node(web)
// 		if ports, ok := b.Value("ports").([]string); ok

// 	}

// }

// func ports(box boxfile.Boxfile) []string {
// 	ports, ok := b.Value("ports").([]string)
// 	if ok {
// 		return ports
// 	}
// 	return []string
// }
